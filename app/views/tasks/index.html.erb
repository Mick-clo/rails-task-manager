<h1>My tasks</h1>

<ul class="form-check">
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task) %>
    <%= link_to "🪦",
  task_path(task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%></li>
  <% end %>
</ul>

<!-- <div class="btn btn-primary text-white"></div> -->
<%= link_to "Create Task", new_task_path %>
